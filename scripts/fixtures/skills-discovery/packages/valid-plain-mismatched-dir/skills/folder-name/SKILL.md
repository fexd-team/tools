---
name: fixture-frontmatter-name
description: Valid skill whose directory name intentionally differs from frontmatter name.
---

# Plain Mismatched Dir

This fixture verifies that install targets use frontmatter `name`.
